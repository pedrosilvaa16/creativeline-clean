"use client";

import Dependency from "@/components/utilities/Dependency";

export default function ClientProviders() {
  // Aqui podes, no futuro, concentrar outros providers/client-only
  return <Dependency />;
}
